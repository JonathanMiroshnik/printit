# Sticker Factory Configuration
# This file contains application settings (non-sensitive)
# API keys should be in .streamlit/secrets.toml

[app]
title = "The Unlimited Dream Company"
privacy_mode = true

# Webhook: send printed image data to an external API
webhook_enabled = true
webhook_url = "https://api.sensorcensor.xyz/festivalin"

[ui]
history_limit = 15
items_per_page = 5
queue_view = true

[txt2img]
# URL for Stable Diffusion API endpoint
url = "http://localhost:7860"

[logging]
file = false
file_level = "WARNING"
stdout = true
stdout_level = "INFO"

[tabs]
# Enable or disable tabs by setting them to true or false
# Order in this list determines tab order in the app
enabled = [
    "Label",
    "Sticker",
    "Sticker Pro",
    "Dog",
    "Cat",
    "History",
    "FAQ",
]
